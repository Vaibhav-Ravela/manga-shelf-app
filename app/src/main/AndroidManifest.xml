<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MangaShelfApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/manga_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/manga_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MangaShelf"
        tools:targetApi="31">
        <activity
            android:name=".presentation.views.activities.HomeScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.views.activities.MangaDetailedActivity"/>
        <activity android:name=".presentation.views.activities.DummyActivity"/>
    </application>

</manifest>